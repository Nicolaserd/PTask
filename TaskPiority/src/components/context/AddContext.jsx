const AddContext = ()=>{
    return(
        <>
        <button
        className="AddContextButton"
        onClick={(event)=>console.log(event)}
        >
            +
        </button>
        </>
    )
}

export default AddContext